type SearchType = {
  no: number;
  size: number;
};

type DateRangeType = {
  startDate: Date;
  endDate: Date;
};

type StatusType = {
  name: string;
  label: string;
};

type AgeType = {
  startAge: number;
  endAge: number;
  label: string;
};

type PriceType = {
  startPrice: number;
  endPrice: number;
  label: string;
};
