type DateRangeType = {
  startDate: Date;
  endDate: Date;
};

type StatusType = {
  name: string;
  label: string;
};

type RatingType = {
  name: string;
  label: string;
};

type AgeType = {
  startAge: number;
  endAge: number;
  label: string;
};

type PriceType = {
  startPrice: number;
  endPrice: number;
  label: string;
};

type GenderType = {
  id: string;
  label: string;
};
