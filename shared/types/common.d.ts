type DateRangeType = {
  startDate: Date | undefined;
  endDate: Date | undefined;
};

type BanModalType = {
  open: boolean;
  selected: string | null;
  status: string | null;
};

type StatusType = {
  name: string;
  label: string;
};

type RatingType = {
  name: string;
  label: string;
};

type GenderType = {
  name: string;
  label: string;
};
